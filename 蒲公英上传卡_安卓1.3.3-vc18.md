# 蒲公英上传卡 · 安卓 1.3.3 (vc18)

> 2026-08-07。EAS 构建 FINISHED(preview profile → APK)。
> APK 已放桌面:`~/Desktop/Bayze-1.3.3-android-vc18.apk`(66 MB)
> SHA256 `b52eb919fe9d69db998c229c7c2f1bfdffcedcf796a95f6e465ad1e61f9e4bda`
> 归档原件:OneDrive `LanWealth/builds/v1.3.3/Bayze-1.3.3-android-vc18.apk`

**本版主角是手机号验证码登录。** 服务端两段式发送已上线并实测发码成功,
安卓侧不需要再等任何东西,可以直接传。

---

## 方式一(推荐,30 秒):命令行直传

在蒲公英「账户设置 → API 信息」复制 API Key,然后终端执行(把 `你的KEY` 替换掉):

```bash
curl -F "file=@$HOME/Desktop/Bayze-1.3.3-android-vc18.apk" -F "_api_key=你的KEY" -F "buildInstallType=1" -F "buildUpdateDescription=$(cat <<'EOF'
Bayze 白泽 1.3.3(Android)

【手机号一键注册登录】
· 新增手机号 + 短信验证码登录,不用记密码
· 没注册过的号码,收到验证码即完成注册
· 登录页/注册页都能切换「邮箱 / 手机号」
· 勾了「记住账号」的,下次自动停在上次用的方式
· 短信验证码目前仅支持中国大陆手机号(11 位);境外号码请用邮箱登录或游客模式

【模型】
· Claude 旗舰档位升级为 Opus 5

【网络】
· 优化国内网络访问的稳定性,主域访问受阻时自动切换备用线路
EOF
)" https://www.pgyer.com/apiv2/app/upload
```

上传成功后返回 JSON 含 `buildShortcutUrl`,确认短链仍为 `bayze-android` 即可。

---

## 方式二:网页上传

1. 打开 https://www.pgyer.com/ → 登录 → 「上传应用」
2. 拖入 `~/Desktop/Bayze-1.3.3-android-vc18.apk`
3. **安装方式**选「公开(任何人可安装)」
4. **更新描述**粘贴下面这段:

```
Bayze 白泽 1.3.3(Android)

【手机号一键注册登录】
· 新增手机号 + 短信验证码登录,不用记密码
· 没注册过的号码,收到验证码即完成注册
· 登录页/注册页都能切换「邮箱 / 手机号」
· 勾了「记住账号」的,下次自动停在上次用的方式
· 短信验证码目前仅支持中国大陆手机号(11 位);境外号码请用邮箱登录或游客模式

【模型】
· Claude 旗舰档位升级为 Opus 5

【网络】
· 优化国内网络访问的稳定性,主域访问受阻时自动切换备用线路
```

5. 发布后确认短链仍是 `www.pgyer.com/bayze-android`

---

## 上传完成后

告诉我一声,我做两件事:

1. 拉蒲公英公开页核验版本号确实是 **1.3.3 / Build 18**
   (⚠️ 它的「最新时间」是相对时间,校验别只 grep 日期 —— 这是 1.3.2 那次踩过的坑);
2. 把官网下载页 `tokenbridge/src/lib/releases.ts` 的安卓条目从 `1.3.2 / vc17`
   改成 `1.3.3 / vc18` 并部署,然后线上复核。

**顺序不能反**:短链 `bayze-android` 永远指向最新版,先改官网会出现
「页面写 1.3.3、下载到 1.3.2」的错位窗口。
