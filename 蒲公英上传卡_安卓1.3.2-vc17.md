# 蒲公英上传卡 · 安卓 1.3.2 (vc17)

> 2026-08-03。实机验证已通过(顶部不遮挡 / 图标不裁切 / 企业工具条正常)。
> APK 已放桌面:`~/Desktop/Bayze-1.3.2-android-vc17.apk`(66 MB,SHA256 `856f8479…07fea8`)
> 归档原件:OneDrive `LanWealth/builds/v1.3.2/Bayze-1.3.2-android-vc17.apk`

---

## 方式一(推荐,30 秒):命令行直传

在蒲公英「账户设置 → API 信息」复制 API Key,然后终端执行(把 `你的KEY` 替换掉):

```bash
curl -F "file=@$HOME/Desktop/Bayze-1.3.2-android-vc17.apk" -F "_api_key=你的KEY" -F "buildInstallType=1" -F "buildUpdateDescription=$(cat <<'EOF'
Bayze 白泽 1.3.2(Android)

【模型全面换代】
· Claude Opus 5 / Sonnet 5(新一代旗舰,编程与长文档更强)
· GPT-5.6 Terra / Sol(1M 上下文)
· Gemini 3.6 Flash / 3.5 Flash-Lite
· 通义 Qwen3.5 系、豆包 2.1、智谱 GLM-5.2
· 共 7 家厂商 21 个型号,一个账号随时切换

【体验修复】
· 修正顶部状态栏遮挡(华为等机型头部按钮不再被压住)
· 修正桌面图标(白泽狮完整显示,不再被裁切)
· 企业工具条启动时偶发不出现 → 自动重试恢复
· 网络容灾:主域访问不畅时自动切换备用入口

【提示】首次使用可用游客模式直接体验;登录后可切换企业空间、关联客户、沉淀企业知识。
EOF
)" https://www.pgyer.com/apiv2/app/upload
```

上传成功后返回 JSON 含 `buildShortcutUrl`,确认短链仍为 `bayze-android` 即可(下载页已指向该短链,无需再改代码)。

---

## 方式二:网页上传

1. 打开 https://www.pgyer.com/ → 登录 → 「上传应用」
2. 拖入 `~/Desktop/Bayze-1.3.2-android-vc17.apk`
3. **安装方式**选「公开(任何人可安装)」
4. **更新描述**粘贴下面这段:

```
Bayze 白泽 1.3.2(Android)

【模型全面换代】
· Claude Opus 5 / Sonnet 5(新一代旗舰,编程与长文档更强)
· GPT-5.6 Terra / Sol(1M 上下文)
· Gemini 3.6 Flash / 3.5 Flash-Lite
· 通义 Qwen3.5 系、豆包 2.1、智谱 GLM-5.2
· 共 7 家厂商 21 个型号,一个账号随时切换

【体验修复】
· 修正顶部状态栏遮挡(华为等机型头部按钮不再被压住)
· 修正桌面图标(白泽狮完整显示,不再被裁切)
· 企业工具条启动时偶发不出现 → 自动重试恢复
· 网络容灾:主域访问不畅时自动切换备用入口

【提示】首次使用可用游客模式直接体验;登录后可切换企业空间、关联客户、沉淀企业知识。
```

5. 发布后确认短链仍是 `www.pgyer.com/bayze-android`

---

## 上传完成后

告诉我一声,我做两件事:
1. 拉取蒲公英公开页核验版本号确实是 1.3.2 / Build 17(注意其「最新时间」显示为相对时间,curl 校验勿只 grep 日期);
2. 官网下载页(releases.ts)已同步改为 1.3.2 / vc17 并部署,届时一并线上复核。
