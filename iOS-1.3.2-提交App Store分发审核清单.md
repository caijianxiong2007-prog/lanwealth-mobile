# iOS 1.3.2 提交 App Store 分发审核 · 操作清单

**2026-08-06** · ASC App ID `6774803277` · 目标 build:**1.3.2 (31)**

---

## 🔴 提交前请先看这条:build 31 缺两个已修好的问题

iOS build 31 是 **8月1日**从 commit `650ff845` 构建的。之后又进了三个 commit,
其中**两个改的是跨平台共享文件,对 iOS 同样生效**:

| commit | 内容 | 对 iOS 的影响 |
|---|---|---|
| `3569515` | 顶部安全区适配 | **有影响**。它把硬编码的 `paddingTop: Platform.OS==='ios' ? 54 : 14` 换成动态 `insets.top + 8` —— build 31 仍是写死的 54px,在灵动岛/无刘海机型上不是最优 |
| `85b018a` | 企业工具条启动期拉取失败自愈(退避重试 5 次 + 回前台补拉) | **有影响**。纯功能修复,与平台无关 |
| `425999e` | Android adaptive 图标 | 无(纯安卓) |

**所以有两条路,请你选:**

| 方案 | 做法 | 代价 |
|---|---|---|
| **A. 按原计划提 build 31** | 直接走下面的步骤 | 公开版 1.3.2 少这两个修复,要等 1.3.3 才补上 |
| **B. 跳过 1.3.2,等 1.3.3** | 1.3.3 内测通过后直接提 1.3.3 分发审核 | 公开版在 1.3.1 多停几天,但一次到位 |

我的看法:**如果 1.3.3 的内测能在几天内跑完,选 B 更干净** —— 少一轮审核,
用户拿到的也是完整版。若急着让商店版跟上宣传口径,选 A 也没问题,那两个
都不是致命问题(安全区只是留白略大;工具条自愈是"失败后能不能自动恢复",
不是"一定失败")。

---

## ✅ 2026-08-06 复核结果(提交前逐条查过)

| 项 | 核实结果 |
|---|---|
| App Store 当前公开版 | **1.3.1**,2026-07-28 上线。已上架 **美国 / 香港 / 新加坡**;**中国区未上架**(iTunes lookup 查 cn 区无结果) |
| build 30 = 1.3.1 | commit `5d40726` ✓ |
| build 31 = 1.3.2 | commit `650ff845` ✓,状态 finished |
| build 31 实际含哪些改动 | 模型换代(Sonnet 5 / GPT-5.6 Terra+Sol / Gemini 3.6+3.5-lite / Qwen3.5 / 豆包2.1 / GLM-5.2)、白泽狮新图标、`X-Bayze-Client` 头(导出引导按端识别)。**下面的发布说明与之逐条对得上** |
| 图标是否是 07-27 那次误用的旧图 | **不是,虚惊**。拉商店实际图标比对过:线上 1.3.1 = **白底蓝色线稿狮**;build 31 = **蓝底白狮全出血** —— 是有意的品牌换代,不是修错图 |
| build 31 是否缺东西 | ⚠️ **缺两个**,见下方红框 |

## 当前状态

- TestFlight:1.3.2 (31) 此前已提交;**1.3.3 (32) 本次已构建并提交**(含手机号验证码登录)。

---

## 步骤(全在 App Store Connect 网页操作)

> ⚠️ `eas submit` **只能把构建上传到 App Store Connect / TestFlight**,
> **不能**提交 App Store 分发审核。建版本、挂构建、填元数据、点提交 —— 只能网页做。

1. 打开 https://appstoreconnect.apple.com/apps/6774803277
2. 左侧 **App Store** 标签 → 若没有 1.3.2 版本,点 **+ 号 → iOS App** → 版本号填 `1.3.2`
3. **「此版本的新增内容」** 贴下面的文案
4. 往下滚到 **「构建」** → 点 **+** → 选 **1.3.2 (31)**
5. **「App 审核信息」→ 备注**:沿用既有说明(见下方「审核备注」)
6. **发布方式**:建议选 **「手动发布此版本」**(审核通过后由你决定上线时间,
   1.3.1 当时就是这么做的)
7. 点右上角 **「添加以供审核」→「提交以供审核」**

---

## 「此版本的新增内容」文案

**中文:**
```
· 模型全面换代:Claude Sonnet 5、GPT-5.6 Terra / Sol、Gemini 3.6 系、Qwen3.5 系、豆包 2.1、GLM-5.2
· 全新品牌图标:蓝底白泽狮
· 导出引导按设备类型自动适配
```

**English:**
```
· Major model refresh: Claude Sonnet 5, GPT-5.6 Terra / Sol, Gemini 3.6 series, Qwen3.5 series, Doubao 2.1, GLM-5.2
· New brand icon
· Export guidance now adapts to your device type
```

---

## 审核备注(App Review Information → Notes)

沿用仓库里既有的两份,按需组合:
- `Apple-Review-Notes-v1.1.txt` —— 多平台已购服务(Guideline 3.1.3)、媒体权限说明
- `Apple-Review-Reply-5.1.1.txt` —— 游客模式(不强制注册即可用 AI 对话)的说明

⚠️ **提审前确认测试账号 `apple-review@lanwealth.com` 处于「有效付费套餐」状态**,
否则审核员只能看到免费模型,体现不出多平台解锁(1.1 那次的教训,已写在
`Apple-Review-Notes-v1.1.txt` 抬头)。

---

## 后续:1.3.3 的公开测试(外部 TestFlight)

按你的顺序,**1.3.3 先内部测试,通过后再推公开测试版**。

- 内部测试员(App Store Connect →「用户和访问」加人,≤100)**免审核**,加完即可安装
- **外部测试员的首个 build 需要过 Beta App Review**(轻量,通常 1 个工作日内)
  → TestFlight 标签 → 外部测试群组 → 添加 build 1.3.3 (32) → 提交 Beta 审核
- 「测试内容」文案见 `发版说明_1.3.3_2026-08-06.md`
