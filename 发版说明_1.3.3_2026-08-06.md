# Bayze 白泽 1.3.3 · 发版说明(iOS + Android)

**2026-08-06** · 上一版 1.3.2(iOS build 31 / Android vc17,基线 commit `3569515`)

---

## 相对 1.3.2 的三项改动

| # | 改动 | commit |
|---|---|---|
| ① | **手机号验证码注册登录**(本版主角) | `4746f51` |
| ② | Claude 最高档换 **Opus 5** | `3c4df08` |
| ③ | API 基址**多域名自动切换**(大陆容灾) | `1c6bf4c` |

② 和 ③ 是 1.3.2 打包**之后**才进的代码,所以随本版才第一次到用户手上。

---

## 面向用户的文案

### TestFlight「测试内容」/ 蒲公英「更新说明」

```
Bayze 白泽 1.3.3

【手机号一键注册登录】
· 新增手机号 + 短信验证码登录,不用记密码
· 没注册过的号码,收到验证码即完成注册
· 登录页/注册页都能切换「邮箱 / 手机号」
· 勾了「记住账号」的,下次自动停在上次用的方式

【模型】
· Claude 最高档升级为 Opus 5

【网络】
· 优化国内网络访问的稳定性,主域访问受阻时自动切换备用线路
```

### 重点测试项(给内测同事)

1. **手机号注册**:用一个**没注册过**的手机号 → 发送验证码 → 收到短信 → 填码 → 应直接进入 App(自动完成注册)
2. **手机号登录**:用**已注册**的手机号重复上面流程,应正常登录到原账号
3. **60 秒冷却**:点完「发送验证码」后按钮应变成倒计时,倒计时结束才能重发
4. **Tab 记忆**:用手机号登录一次 → 退出 → 重开 App,登录页应默认停在「手机号」Tab
5. **邮箱登录没被弄坏**:原来的邮箱+密码登录、忘记密码、游客模式,都要照常可用
6. **手机号格式**:11 位国内号直接输(自动补 +86);境外号需带 `+区号`
7. **弱网/断网**:发码时断网,应给出可读的错误提示,不是白屏或卡死

---

## ⚠️ 测试前置条件

手机号入口是**服务端开关**控制的(`/api/app-config` 的 `phoneAuth`)——
已确认生产返回 `"phoneAuth": true`,所以入口会显示。

若测试时看不到「手机号」Tab,按顺序排查:
1. 杀掉 App 重开(开关有本地缓存,首次安装需要拉一次配置)
2. `curl https://app.lanwealth.com/api/app-config` 确认仍是 `"phoneAuth":true`
3. 手机能否上外网(拉配置要连我们的域;登录本身走 Supabase 直连域,不受影响)

**若发码报错**,那是 Supabase Phone provider / 短信 Hook 侧的问题,不是 App 的问题
—— 网页端 `app.lanwealth.com/auth/login` 的手机号 Tab 用同一条链路,可以拿它对照验证。
