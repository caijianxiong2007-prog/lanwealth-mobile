# 手机版 1.3.5 发版说明(2026-08-29)

## 本版内容(用户可见)

1. **新增「工具」Tab —— 实用工具导流入口**(获客工具矩阵落地到手机端)
   - 底部导航新增「Tools」标签,内含三张卡片,点击在系统浏览器打开网页版工具:
     - **拍照文字识别(OCR)**:图片/拍照/截图/PDF → 文字并结构化,可导 Excel/Word。手机浏览器文件框可直接调相机,拍张单据即识别。
     - **AI 文档翻译**:整篇文档翻译保排版。
     - **录音转写 + AI 会议纪要**:录音 → 转写 + 结构化纪要(议题/结论/待办)。
   - 识别/翻译/纪要结果,登录后可一键存入企业知识库、客户记忆。
   - 工具页经 `resolveBase()` 打开(多域名自动切换,规避大陆 SNI 阻断)。

## 技术要点

- `app/(tabs)/tools.tsx`:新增工具 Tab;`resolveBase()` + `Linking.openURL` 打开 `/tools/ocr|translate|transcribe`。
- `app/(tabs)/_layout.tsx`:Tab 显示由远程配置 `tools` 控制(默认开,`href:null` 隐藏);新增 🧰 图标。
- `lib/appConfig.ts`:`AppConfig` 加 `tools` 字段(**默认开**,与 phoneAuth 相反;仅服务端显式 `tools:false` 才隐藏)。
- 服务端 `tokenbridge` `/api/app-config`:下发 `tools`(默认开;env `MOBILE_TOOLS_TAB=0` 可远程关,免重新上架)。commit `971ac19`。
- **OTA 热更新(EAS Update)接入**:
  - `expo-updates ~0.28.18`;`app.json` → `updates`(url `u.expo.dev/262711cc-a1c2-4fd8-9e77-ad482072c6de`,`ON_LOAD`)+ `runtimeVersion { policy: fingerprint }`。
  - `eas.json` 各 profile 加 `channel`(production/preview)。
  - ⚠️ 接 OTA 本身是原生改动,故本版为"最后一次为工具入口过审";**本版上线后**,纯 JS 改动 `eas update --branch production` 免过审热推,原生/权限改动仍需重打包(fingerprint 变)。
  - 详见 `OTA热更新说明.md`。

相关 commit:`184549a`(Tools Tab)/`794f812`(OTA)/`4703d56`(远程开关)/`a088ec2`(bump 1.3.5)。

## 发版记录(2026-08-29)

| 渠道 | 版本/构建 | 动作 | 状态 |
|---|---|---|---|
| iOS App Store | **1.3.4 (build 33)** | 提交分发审核(用现成 TestFlight 构建,不重打) | 正在等待审核(≤48h) |
| iOS TestFlight | **1.3.5 (build 34)** | 上传 + 加入外部「团队测试群」提交 Beta 审核 | 正在等待审核(Beta ≤24h) |
| 安卓 蒲公英 | **1.3.5 (versionCode 20)** | 上传 + 发布 | 已发布,可测 |

- **EAS 构建**:iOS `36e01cf3-1513-4d40-a60f-b5bd0d4cd61d`;安卓 `8275532a-a5b8-4777-ab72-6939269b90b9`。SDK 53.0.0。
- **iOS 提交**:`eas submit`,用 EAS 服务器存的 ASC API Key(`[Expo] EAS Submit`),未用 Apple 密码。
- **TestFlight 外部群公开链接**:https://testflight.apple.com/join/WDVGhr5Q
- **ASC App**:6774803277(Bayze AI)。
- **成品归档**:`OneDrive-个人/LanWealth-Global/LanWealth/builds/`
  - `Bayze-1.3.5-ios-build34.ipa`(15.5MB)
  - `Bayze-1.3.5-android-vc20.apk`(73MB)

## 各处发版说明文案(已提交,存档备查)

**App Store 更新说明(1.3.4,用户确认照用)**
> 本次更新:
> · 分享更好用:从备忘录 / 微信 / 文件分享进来,可选「加入对话」或「存入知识库」;文档和图片自动提取文字(照片自动转码),存入后 AI 随时引用。
> · 对话更稳定:长时间无响应会明确提示并可重试;生成过程中可随时停止,已生成内容保留。

**蒲公英 Release Notes(1.3.5)**
> 本次更新:
> · 新增「工具」入口:手机上直接使用拍照文字识别、AI 翻译、录音转写;识别结果登录后可一键存入企业知识库、客户记忆。
> · 底层升级:支持后续功能快速迭代更新。

**TestFlight 测试内容(1.3.5)**
> 本版新增「工具」标签,请重点测试:①拍照/截图文字识别 ②文档翻译 ③录音转写+AI 会议纪要。其余功能(AI 对话、分享收纳、存入知识库)如常,留意稳定性。无需登录即可试用工具;企业功能需登录。

## 后续(发版后热更流程)

- 本版商店版上线后,JS/文案/样式改动:`eas update --branch production --message "…"`(先 `--branch preview` 灰度)。用户冷启动自动生效。
- 回滚:`eas update:rollback` 或重推上一个好版本。
- 踩坑备忘:
  - `eas build:download` 给的是解包 `.app`,取真 `.ipa` 用 `eas build:list --platform ios --json` 读 `artifacts.applicationArchiveUrl` 再 `curl`。
  - 蒲公英只收文件(不支持 URL 导入);浏览器自动化上传上限 10MB,大 APK(73MB)需手动上传或走蒲公英 API。
  - macOS 无 `timeout` 命令;`eas` 命令用 `--non-interactive` 防交互卡住。
