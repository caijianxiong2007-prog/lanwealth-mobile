# 手机版 1.3.4 发版说明(2026-08-18)

## 本版内容(用户可见)

1. **分享收纳更能干**(备忘录上传需求,用户反馈驱动)
   - 从备忘录/微信/文件等 App 分享内容进来时,新增去向选择:**加入对话** 或 **存入知识库**(短文本仍直接进输入框)。
   - 存入知识库:文档和图片自动抽取文字(图片走 OCR,iPhone 默认的 HEIC 照片自动转码),企业成员默认存企业知识库,未加入企业自动存个人库;存入后 AI 对话随时可引用。
   - iOS 分享面板一次最多可收 5 个附件(原 1 个);无后缀的文本附件(备忘录导出常见)也能识别。

2. **对话更稳**(对齐 Web 端 2026-08-18 事故整改)
   - 模型长时间无响应会明确报错并可重试,不再无限转圈。
   - 生成过程中发送键变**停止按钮**,随时可停,已生成的部分保留。
   - 空回复不再留下"假生成中"的气泡。

## 技术要点

- `lib/api.ts`:streamChat 加 330s 停滞看门狗(每次进展重置,不腰斩长生成)、`stopStream()`/`USER_STOP` 哨兵、`saveToKnowledge()`(POST /api/knowledge,org→403 自动落 personal)。
- `app/(tabs)/index.tsx`:share-intent 消费改三选;addDocument 白名单外 UTF-8 嗅探(前 2000 字无 NUL);HEIC→JPEG 走 expo-image-manipulator(新增依赖 ~13.1.7);send() 空正文回滚+报错。
- `app.json`:`NSExtensionActivationSupportsFileWithMaxCount` 1→5;version 1.3.4(buildNumber/versionCode 由 EAS remote 自增)。
- 服务端无需配套改动(/api/extract、/api/knowledge 均为既有接口;聊天稳定性同时受益于 tokenbridge 4cc44a6 的 300s/首块期限)。

## 真机验证清单(交老板)

- [ ] 备忘录:选一条**文字+照片混排**的笔记 → 分享 → Bayze:应弹出"收到分享内容"三选。
- [ ] 选「存入知识库」→ 提示"N 项已存入企业知识库" → 网页端知识库能看到,照片变成了可搜文字。
- [ ] 选「加入对话」→ 附件挂上,正常提问。
- [ ] 对话中点 ■ 停止:立即停,已生成文字保留。
- [ ] （可选)飞行模式发消息:应见明确报错+重试,不是无限转圈。

## 发版操作

- iOS:`eas build --platform ios --profile production` → `eas submit -p ios`(TestFlight,审核约 1-3 天)。
- 安卓:`eas build --platform android --profile preview`(APK)→ 蒲公英。
- 跨境:`EAS_SKIP_AUTO_FINGERPRINT=1`;成品归档 OneDrive `LanWealth/builds/`。
